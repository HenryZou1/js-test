export function Reduce(array, reducer, initialValue) {
  // TODO: write your code here
  	//for(let i = 0; i < exampleArray.length ;i++){
	//	 num =exampleReducer(num,exampleArray[i]);
	//}
	//return num;
	return array.reduce(reducer, initialValue);
}
